import ReactMarkdown from "react-markdown";
import React from "react";
import "./md.css";

export const ReactMarkdown = (props: any) => {
    return <ReactMarkdownComponent className="md" {...props} />;
};


